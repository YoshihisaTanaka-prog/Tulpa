<%= form_with(model: user_info) do |form| %>
  <% if user_info.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_info.errors.count, "error") %> prohibited this user_info from being saved:</h2>

      <ul>
        <% user_info.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :食事が必要, style: "display: block" %>
    <%= form.check_box :is_need_meal %>
  </div>

  <div>
    <%= form.label :睡眠が必要, style: "display: block" %>
    <%= form.check_box :is_need_sleep %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
