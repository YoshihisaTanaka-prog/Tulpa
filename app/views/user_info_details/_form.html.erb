<%= form_with(model: user_info_detail) do |form| %>
  <% if user_info_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_info_detail.errors.count, "error") %> prohibited this user_info_detail from being saved:</h2>

      <ul>
        <% user_info_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :種類分け, style: "display: block" %>
    <select name="user_info_detail[category_id]" id="user_info_detail_category_id">
    <option value="0">選択してください</option>
      <% UserInfoCategory.all.each do |category| %>
        <option value="<%= category.id %>"><%= category.type_name %></option>
      <% end %>
    </select>
  </div>

  <div>
    <%= form.label :特徴, style: "display: block" %>
    <%= form.text_field :text %>
  </div>
  <%= form.hidden_field :user_info_id %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
